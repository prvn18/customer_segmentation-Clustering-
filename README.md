# customer_segmentation-Clustering-
Customer segmentation project using K-Means clustering. The pipeline handles missing values, scales numeric features, encodes categorical data, selects optimal clusters via silhouette score, and visualizes segments with PCA. Demonstrates end-to-end unsupervised learning for customer analysis.
